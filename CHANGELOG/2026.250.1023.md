# 2026.250.1023 — feat: crabbox CLI v0.51.0 (no-hostname surface docs + deploy-add fix)

## Summary

Bumps the crabbox CLI candy to **v0.51.0** (upstream release carrying the merged #1924 no-hostname implementation; credited #1813/#1922): `CRABBOX_VERSION 0.48.1 -> 0.51.0`, assertions + skill docs updated, and the CC-2 dead word `charly fleet add` in the skill's install example fixed to `charly deploy add`.

The config FILE name was R1-verified against the v0.51.0 binary: the CLI reads `~/.config/crabbox/config.yaml` (a `crabbox.yaml` in that dir is IGNORED) — the docs state the working path.

## How tested

Fresh-rebuild bed run on the `disposable: true` target `check-crabbox-pod` (repo overrides feed the PR branches into the build; the binary is CalVer-stamped):

```
EVAL BED: check-crabbox-pod — exit 0 (13/13 steps)
  [image-build] ok 47.2s  [check-image] ok 19.0s  [deploy-add] ok 4.2s   [config] ok 5.7s
  [start] ok 3.4s         [bring-up-members] ok    [check-live] ok 12.3s  [feature-run] ok 10.7s
  [update] ok 26.5s       (R10 fresh-rebuild gate) [check-live-rebuild] ok  [feature-run-rebuild] ok
  [cleanup] ok            [cleanup-members] ok
PASS [cbx-cli-binary]      ok
PASS [cbx-verb-version]    0.51.0          <- in-venue crabbox --version (v0.51.0 binary)
PASS [cbx-verb-providers]  local-container matrix present
PASS [cbx-verb-config]     config=/home/user/.config/crabbox/config.yaml
PASS [cbx-verb-doctor]     ok
```

The version assertion in the candy plan (`0.51.0`) matches the live run output. See pod-crabbox#3 for the e2e pairing proof (coordinator v0.51.0 + full local loop).

## Rulebook compliance
- R0 (skills honored): the layer-authoring skill (/charly-image:layer) was loaded before the candy edit; the git-workflow + go skills governed the PR and the R1 live binary probe; the egress skill informs the config-surface docs.
- R1: the config filename + noHostname behavior verified against the real v0.51.0 binary; the CC-2 dead-word doc fixed; no anomaly left unexplained.
- R4a (docs never route around a defect): the docs edits FIX the CC-2 dead word (documentation was stale, now corrected) and document the verified config.yaml path — documentation is aligned with reality, never a workaround.
- R7: live bed output pasted above (exit 0, 13/13).
- R10: fresh-rebuild disposable proof on the final tree (update leg re-verified the rebuilt image).
- R2/R3/R4/R5/R6/R8/R9: N/A or satisfied — single candy pin + docs; no parallel mechanism (R3); no workarounds (R4); no legacy deletion (R5); branch-per-change PR, no force-push (R6); no generated Go/artifacts (R8/R9).

## Change classification

- **Class:** candy pin alignment + docs. **Verification gate:** fresh-rebuild disposable bed (exit 0, 13/13) + live binary probe. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*